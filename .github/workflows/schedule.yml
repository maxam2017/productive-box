name: Update gist
on:
  repository_dispatch:
    types: [build-event]
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{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}}
          GIST_ID: a2734812069679fe20a6d1f906b38fa7
          TIMEZONE: Asia/Seoul
